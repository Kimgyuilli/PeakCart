apiVersion: v1
kind: ConfigMap
metadata:
  name: peekcart-config
  namespace: peekcart
data:
  SPRING_PROFILES_ACTIVE: k8s
  # Phase 3 Task 3-4 부하 테스트 — 캐싱 전/후 TPS 비교용 토글 (CacheConfig @ConditionalOnProperty)
  # baseline 측정 시 "false" 로 전환 후 재배포, 정상 운영 기본값은 "true"
  PEEKCART_CACHE_ENABLED: "true"
