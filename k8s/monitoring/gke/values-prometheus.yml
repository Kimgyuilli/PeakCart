# kube-prometheus-stack Helm values — GKE 환경 (TODO)
#
# 본 파일은 ADR-0006 불변식 6 에 따른 명시적 TODO 입니다.
# Task 3-4 Step 0 에서 GKE Standard e2-standard-4 환경에 맞춰 작성합니다.
#
# 작성 시 고려 사항:
# - prometheus.prometheusSpec.retention: 부하 테스트 사후 분석 범위에 맞춰 결정 (>= 24h 권장)
# - prometheus.prometheusSpec.resources.limits: e2-standard-4 (4 vCPU / 16Gi) 기준 상향
# - prometheus.prometheusSpec.storageSpec: GKE StorageClass `standard-rwo` 기반 PVC
# - grafana.service.type: Internal LoadBalancer (annotation:
#     networking.gke.io/load-balancer-type: "Internal")
# - 리소스 합산 총량은 노드 capacity 와 PeekCart Pod 자원 예약을 함께 고려
#
# 작성 후 본 헤더의 (TODO) 표기를 제거하고 install.sh 의 exit 1 을 해제합니다.
