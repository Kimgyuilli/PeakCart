apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-config
  namespace: peekcart
data:
  # 연결 정보(업스트림 URI·JWKS·Redis)는 gateway 모듈의 application-k8s.yml 이 소유한다(ADR-0007).
  # ConfigMap 은 그 프로파일을 켜는 스위치만 담는다 — 도메인 5서비스와 동형.
  SPRING_PROFILES_ACTIVE: k8s
